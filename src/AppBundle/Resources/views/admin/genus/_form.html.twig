{{ form_start(genusForm) }}
    {{ form_row(genusForm.name) }}
    {{ form_row(genusForm.subFamily) }}
    {{ form_row(genusForm.funFact) }}
    {{ form_row(genusForm.speciesCount) }}
    {{ form_row(genusForm.isPublished) }}
    {{ form_row(genusForm.firstDiscoveredAt) }}
<h3>Scientists</h3>
<div class="row js-genus-scientist-wrapper">
    {% for geusScientistsForm in genusForm.genusScientists %}
        <div class="col-xs-4 js-scientist-item">
            <a href="#" class="js-remove-scientist pull-right"><span class="fa fa-close"></span></a>
            {{ form_errors(geusScientistsForm) }}
            {{ form_row(geusScientistsForm.user) }}
            {{ form_row(geusScientistsForm.yearsStudied) }}
        </div>
    {% endfor %}
</div>
<button type="submit" class="btn btn-success" formnovalidate>Save</button>
{{ form_end(genusForm) }}